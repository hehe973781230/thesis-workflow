# MBA Thesis Workflow - Python 依赖
# 安装：pip install -r requirements.txt

# ============================================================
# 核心依赖（必须）
# ============================================================
python-docx~=1.1

# 可选重型依赖见 requirements-optional.txt（如需 Phase 1.3 向量加速）
